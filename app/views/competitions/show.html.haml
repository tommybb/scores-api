%p#notice= notice

%p
  %b Name:
  = competition.name

= link_to 'Edit', edit_competition_path(competition), class: 'btn btn-sm btn-primary'
= link_to 'Back', competitions_path, class: 'btn btn-sm btn-primary'
